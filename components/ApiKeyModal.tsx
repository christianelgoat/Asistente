import React from 'react';

// FIX: Per security and API guidelines, the application must not prompt the user for an API key.
// The API key should be securely managed on the server side using environment variables.
// This component has been disabled to align with best practices.
const ApiKeyModal: React.FC = () => {
  return null;
};

export default ApiKeyModal;
